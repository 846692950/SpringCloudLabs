spring:
  application:
    name: gateway
  cloud:
    nacos:
      server-addr: 43.142.106.101:8848
      config:
        file-extension: yaml
        shared-configs:
          - dataId: shared-log.yaml # 共享日志配置